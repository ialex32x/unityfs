using System;
using System.IO;
using System.Collections.Generic;

namespace UnityFS
{
    using UnityEngine;

    public class Sample : MonoBehaviour
    {
        void Awake()
        {
            // load local file registry from direct filesystem
            // load remote manifest
            // check diff local/remote manifest, update local manifest
            // # load sync
            // # load async
        }
    }
}
